<?xml version="1.0" encoding="UTF-8" ?>
<Module>
	<ModulePrefs title="CountUP" 
		height="20" width="220" scaling="false"
		author="Salman Esmaili" 
    author_email="webdev@esmaili.ws"
		screenshot="http://salman.esmaili.ws/countdown/counter.png"
		thumbnail="http://salman.esmaili.ws/countdown/countert.png"
		description="Counting up from the first day!!!">
		<Require feature="dynamic-height"/>
    <Require feature="setprefs" />
	</ModulePrefs>
	<Content type="html">
		<![CDATA[ 
<SCRIPT type="text/javascript">
function setcountup(theyear, themonth, theday) {
    yr = theyear;
    mo = themonth;
    da = theday
}

setcountup(2006, 09, 15)

//STEP 2: Configure text to be attached to count up
var displaymessage = ""

//STEP 3: Configure the below 5 variables to set the width, height, background color, and text style of the countup area
var countupwidth = '350px'
var countupheight = '35px'
var countupbgcolor = 'white'
var opentags = '<font color="black">'
var closetags = '</font>'
var boldme = '<B>'
var unboldme = '</B>'

//////////DO NOT EDIT PASS THIS LINE//////////////////

var montharray = new Array("Jan", "Feb", "Mar", "Apr", "May", "Jun", "Jul", "Aug", "Sep", "Oct", "Nov", "Dec")
var crosscount = ''

function start_countup() {
    if (document.layers)
        document.countupnsmain.visibility = "show"
    else if (document.all || document.getElementById)
        crosscount = document.getElementById && !document.all ? document.getElementById("countupie") : countupie
    countup()
}

if (document.all || document.getElementById)
    document.write('<span id="countupie" style="width:' + countupwidth + '; background-color:' + countupbgcolor + '"></span>')

window.onload = start_countup


function countup() {
    var today = new Date()
    var todayy = today.getYear()
    if (todayy < 1000)
        todayy += 1900
    var todaym = today.getMonth()
    var todayd = today.getDate()
    var todayh = today.getHours()
    var todaymin = today.getMinutes()
    var todaysec = today.getSeconds()
    var todaystring = montharray[todaym] + " " + todayd + ", " + todayy + " " + todayh + ":" + todaymin + ":" + todaysec
    paststring = montharray[mo - 1] + " " + da + ", " + yr
    dd = Date.parse(todaystring) - Date.parse(paststring)
    dday = Math.floor(dd / (60 * 60 * 1000 * 24) * 1)
    dhour = Math.floor((dd % (60 * 60 * 1000 * 24)) / (60 * 60 * 1000) * 1)
    dmin = Math.floor(((dd % (60 * 60 * 1000 * 24)) % (60 * 60 * 1000)) / (60 * 1000) * 1)
    dsec = Math.floor((((dd % (60 * 60 * 1000 * 24)) % (60 * 60 * 1000)) % (60 * 1000)) / 1000 * 1)

    if (document.layers) {
        document.countupnsmain.document.countupnssub.document.write(opentags + dday + " D, " + dhour + " H, " + dmin + " M, " + dsec + " S " + displaymessage + closetags)
        document.countupnsmain.document.countupnssub.document.close()
    } else if (document.all || document.getElementById)
        crosscount.innerHTML = opentags + boldme + dday + unboldme + " D, " + boldme + dhour + unboldme + " H, " + boldme + dmin + unboldme + " M, " + boldme + dsec + unboldme + " S. " + displaymessage + closetags

    setTimeout("countup()", 1000)
}
</SCRIPT>
				
     	]]>
  	</Content>

</Module>